version: "3.9"
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: api-post:latest
    container_name: api-post
    env_file: .env
    ports:
      - "3000:3000"
    restart: unless-stopped
